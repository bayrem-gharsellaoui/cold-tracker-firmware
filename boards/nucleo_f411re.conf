# PPP over ST-LINK VCP UART
CONFIG_NET_DRIVERS=y
CONFIG_NET_L2_PPP=y
CONFIG_NET_PPP=y
CONFIG_NET_DEFAULT_IF_PPP=y

# UART console/shell must not use the PPP UART
CONFIG_UART_CONSOLE=n
CONFIG_SHELL_BACKEND_SERIAL=n
